
bh-ce.py
```
bloodhound-ce.py --zip -c All -d "tombwatcher.htb" -p 'H3nry_987TGV!' -u "henry" -ns 10.10.11.72b
```
 rusthound
```
  
rusthound-ce --domain tombwatcher.htb  -u henry -p 'H3nry_987TGV!' -c All --zip          
---------------------------------------------------
```

or netexec bloodhound
```
nxc ldap DC01.ad.someorg.local -d ad.someorg.local -u 'hporter' -p 'Gr8hambino!' --dns-server 172.16.8.3 --bloodho  
und

```shell-session
$ sudo bloodhound-python -u 'forend' -p 'Klmcargo2' -ns 172.16.5.5 -d ad.someorg.local -c all 

zip -r ilfreight_bh.zip *.json
```

```
 
 `running the SharpHound.exe collector from the MS01 attack host.`
```powershell-session
PS C:\htb> .\SharpHound.exe -c All --zipfilename ILFREIGHT
```

also manually zip
```
`Compress-Archive -Path *.json -DestinationPath sharpdump.zip`
```


## AD Data Collection Methods

### soap: used in soaphound,soaPy, and rusthound uses the ADWS protocol to query ldap
![[Pasted image 20250731140745.png|1000]]
 
